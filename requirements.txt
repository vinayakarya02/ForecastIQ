# ForecastIQ runtime dependencies (Streamlit app + engines).
# Dev/optional tooling (pytest, ruff, notebook stack) lives in pyproject extras:
#   pip install -e ".[dev]"
# Validated on pandas 3.0 / numpy 2.4; also compatible with pandas 2.1+.

# ---- Core data stack ----
pandas>=2.1
numpy>=1.24
python-dateutil>=2.8

# ---- Database ----
SQLAlchemy>=2.0,<3.0
# psycopg2-binary>=2.9        # uncomment to use PostgreSQL instead of SQLite

# ---- Statistics & forecasting ----
statsmodels>=0.14
scikit-learn>=1.3

# ---- Visualization ----
matplotlib>=3.7
plotly>=5.18

# ---- Interactive app ----
streamlit>=1.40

# ---- Config, IO, Excel ----
PyYAML>=6.0
openpyxl>=3.1                 # read/write .xlsx (generated sample + workbooks)
xlrd>=2.0                     # read legacy .xls (real Global Superstore file)
tabulate>=0.9                 # pretty validation reports
