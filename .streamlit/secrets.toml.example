# Copy to .streamlit/secrets.toml for deployment. Optional — the app defaults to the
# local SQLite warehouse. Override the database only if you host it elsewhere.
#
# FORECASTIQ_DB_URL = "sqlite:///forecastiq.db"
# FORECASTIQ_DB_URL = "postgresql+psycopg2://user:password@host:5432/forecastiq"
